# lastName_firstInitial_HW2
In class, we built a reusable Counter class using JavaScript OOP principles. The client is requesting additional features be added to the counter.  This assignment extends that work by reinforcing:  Encapsulation State-driven UI updates DOM manipulation inside a class Separation of concerns (mount() vs update()) Technical documentation.
